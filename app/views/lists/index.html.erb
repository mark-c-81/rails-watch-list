<div class="container">
	<div class="row">
		<% @lists.each do |list| %>
			<div class="col-4">
				<div class="card">
					<div class="card-body">
						<h3 class="card-title"><%= list.name %></h3>
					</div>
				</div>
			</div>	
		<% end %>
	</div>
</div>